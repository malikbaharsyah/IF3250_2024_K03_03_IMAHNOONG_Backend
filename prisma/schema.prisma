generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model something {
  some_id     Int    @id @unique @default(autoincrement())
  somename    String
  somelut     String @unique
  somer       String
  someaddress String
}
